node_modules/

# Build output and generated files. These are regenerated by `npm run dev`
# or `npm run build`; committing them causes conflicts between machines.
.next/
out/
next-env.d.ts
*.tsbuildinfo
